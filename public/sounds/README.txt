Place your audio files in this directory to be played by the profile modal.

Required files:
1. `birthday.mp3` - Plays when it is the user's birthday.
2. `default-ambient.mp3` - The default background song.

You can also name specific songs and update `components/profile/ProfileModal.tsx` to conditionally load them based on the user!
